**********11-8-26************
list methods:
1)operations:
--concatenation
--reptation
--indexing
--slicing
--membership
2)Methods:
--max(a)
--min(a)
--sum()
--sorted(a)
--revrse()-to reverse a list
--len(a)
--a.append
--a.insert(with index value)
--extend(it will add list of elements to last)
--pop(it will del last element)
--pop()(with indexing
--remove
--del (with indexing)
--clear(it will remove all elements from list)
--.index
--.count
--.copy-
--any()(any one is true value)
--all()(all values nedds to be true)




need to check
diff b/w sort and sorted 